# Growth Notes

Short log of style observations per day. A theme is only promoted to a "pattern" after the same kind of issue appears on 3+ separate days.

## Single-day observations (not yet patterns)

- **Day 7 — Month 2 Week 2 Day 1 (Maximum Likelihood Estimation, python, intermediate):** Variable-naming clarity noted — cryptic locals `fst`/`scd`/`std` in `log_likelihood.py:41-45` and typo/truncated names `cureent_mu`/`log_likehd` in `mle_fit_gaussian.py:69-81`. Also `range(len(data))` indexing instead of direct iteration `for x in data`, error-message typos (`"data is empy"` `log_likelihood.py:37`, `"sigma cant be lessthan..."` `log_likelihood.py:35`), inconsistent punctuation/spacing, unnecessary list in `sum([...] for ...)` `mle_fit_gaussian.py:37`, and dead commented code ` # raise NotImplementedError`. No recurring pattern claimed yet — first observation of this type in available history.

- **Day 8 — Month 2 Week 2 Day 2 (MAP Estimation & Priors, python, intermediate):** Error handling is the main item — four bare `raise ValueError` with no message (`map_fit_gaussian.py:40,73-74,110,148`), inconsistent with the learner's own Day-1 style (`log_likelihood.py:20` does include messages). Also single-line `if ... : raise` bodies, a dead `n = len(data)` (`:149`), `np.arange` with a computed float step where the docstring promises inclusive endpoints (`:159-160`, so `mu_range[1]` is never evaluated), numpy pulled in for a grid the spec scoped to plain Python (`:23`), the `# MAP == L2` comment stranded below a `return` (`:78-79`), `sum([...])` list instead of a generator (`:113`), and `denomenator` misspelt (`:116`). No pattern claimed — see count note below.

**Second-occurrence counts (day 7 + day 8, still below the 3-day threshold):** identifier/message typos (`cureent_mu`, `log_likehd`, `"data is empy"` → `denomenator`) — 2 days; `sum([...])` list where a generator would do — 2 days; grid-bounds handling that misses an endpoint (`step = (max-min)/steps` with a range that stops short) — 2 days. Recorded as counts only, not as trends. If any of these appears again on a third day, promote it below.

- **Day 9 — Month 2 Week 2 Day 3 (Shannon Entropy, python, intermediate):** Clear improvement on Day 8's headline item — every `raise` now carries a message, and `entropy.py:41` uses `sum(... generator ...)` correctly. New single-day items: duplicate `import math` (`entropy.py:11,13`); guard clauses chained with `elif` instead of sequential `if`s (`:32-39`); uppercase accumulator `H` (`:41`); helper misnamed `pdf` for a discrete empirical distribution, missing return annotation and docstring (`file_entropy.py:22-26`); fragile `from entropy import entropy` that only resolves under `code/` as cwd (`:19`); redundant `except FileNotFoundError: raise FileNotFoundError(...)` without chaining (`:67-68`); char tokens derived from word tokens so spaces silently leave the char alphabet (`:55-64`); leftover `# TODO(student)` scaffold in the shipped module docstring (`:9`); message-wording slips (`"one or more entry..."`, `"The pmf not sum up to 1"`, `"The base is less than 0"` which also fires at 0); PEP 8 spacing slips (`p*math.log(p , base)`, `open(path , ...)`, `try :`, `elif level==`). Doc-code mismatch: docstring promises sum-tolerance `1e-9`, code enforces `0.001` (`entropy.py:20-21` vs `:38`).

**Day 9 delta on the counts (explicitly not promotions):** literal identifier/message spelling typos stay at 2 days — Day 9 has message-grammar slips but no misspelt identifier (adjacent, not a clean third hit); `sum([...])` stays at 2 days with the streak broken, logged as an improvement; grid-endpoint handling stays at 2 (no grid on Day 9). New watch item at count 2: docstring numeric promise vs code behaviour (Day 8 inclusive-endpoint docstring vs `np.arange`; Day 9 `1e-9` vs `0.001`) — needs a third day before it becomes anything.

- **Day 10 — Month 2 Week 2 Day 4 (Cross-Entropy & KL Divergence, python, intermediate):** Sustained improvement — every `raise` carries a message (Day 9's fix for Day 8 holds) and all three public functions have docstrings plus full type hints. New single-day items: sibling functions duplicate validation then diverge — `cross_entropy()` (`cross_entropy.py:8-18`) validates emptiness/length/normalization/non-negativity while `kl_divergence()` (`:31-41`) validates none of them and re-implements the same loop body (suggest a shared `_validate_p_q` helper); exact float comparison `sum(q) != 1` (`:14`, use `math.isclose` with a documented tolerance); guard order in `binary_log_loss()` checks length-mismatch before emptiness (`log_loss.py:9-16`); cross-file import-convention drift (`import math` vs `from math import log2`); cryptic/abbreviated locals `sol` and `b_log_loss_list` (`:18-23`); inaccurate guard message `"both p and q are empty"` which fires when either is empty (`cross_entropy.py:9`).

**Day 10 delta on the counts (promotions below):** literal misspelt-identifier typos stay at 2 days (days 7, 8) — Day 10 has grammar slips (`"an value"` ×2, `"are not sum up of 1 "`) but no misspelt identifier, so that narrow count does not move. Four watch items reach 3 days and are promoted to Themes: leftover scaffold in the shipped file (days 7, 9, 10); unnecessary intermediate list for a reduction (days 7, 8, 10 — Day 9 used a generator correctly); docstring promise vs code behaviour (days 8, 9, 10); PEP 8 whitespace slips (days 7, 9, 10). Error-message wording (grammar/capitalization/accuracy, days 7, 9, 10) is also promoted, defined separately from the narrower identifier-spelling count. Grid-endpoint handling stays at 2 (no grid on Day 10). New watch item at count 2: UPPERCASE accumulator for a plain local (`H` day 9; `Output`/`KL_D` day 10) — needs a third day.

- **Day 11 — Month 2 Week 2 Day 5 (Mutual Information, python, intermediate):** Sustained good — error contracts in both docstrings, precise `Sequence[Hashable]` hints, a seeded two-experiment `__main__` demo, demo-only imports deferred under `__main__`. New single-day items: Cartesian-product loop with an `if joint_count > 0` guard instead of iterating observed pairs (`mutual_information.py:36-40`); `Counter` imported from `typing` instead of `collections` (`:5`); `range(len(...))` indexing in three places where `zip`/`enumerate` is idiomatic (`mutual_information.py:32`, `feature_selection.py:33-43`); duplicated top-level + `__main__` import of the same name propped up by a `sys.path` shim (`feature_selection.py:4` vs `:54-56`); verb-named `rank` list and positional `base` threading (`:39-44`); magic demo literals `Random(11)`/`n = 200`/`0.10` (`:59-62`); message typos `"the fuction requre equal length"`, `"the base is ... that the fuction doesn't accept"`, `"same lenth"` (`mutual_information.py:23,25`, `feature_selection.py:31`).

**Day 11 delta on the counts (promotions below):** genuine spelling typos in messages (`fuction` ×2, `requre`, `lenth`) are a clean third hit for the narrow identifier/message-spelling count (days 7, 8, 11 — days 9, 10 had grammar slips only), so it is promoted and folded into the wording Theme. UPPERCASE-accumulator watch reaches 3 days (`H` day 9, `Output`/`KL_D` day 10, `MI` day 11) and is promoted. `range(len(...))` indexing reaches 2 days (days 7, 11) — recorded as a count only. Grid-endpoint handling stays at 2 (no grid on Day 11).

## Themes (promoted after 3+ days)

- **Leftover scaffold / dead code in the shipped file (days 7, 9, 10, 11):** Day 7 dead commented `# raise NotImplementedError`; Day 9 leftover `# TODO(student)` in the module docstring; Day 10 `__main__` block that only raises `NotImplementedError` (`log_loss.py:26-27`); Day 11 dead commented-out probe (`mutual_information.py:53`). Remove the stub or finish it before shipping.
- **Unnecessary intermediate list for a sum/reduction (days 7, 8, 10):** Day 7 `sum([...] for ...)`; Day 8 `sum([...])`; Day 10 append-into-`b_log_loss_list`-then-`sum` loop (`log_loss.py:18-23`). Default to a generator or running accumulator; Day 9's `sum(... generator ...)` is the in-history example of the fixed form.
- **Docstring promise vs code behaviour (days 8, 9, 10):** Day 8 inclusive-endpoint docstring vs `np.arange`-excluded end; Day 9 documented `1e-9` tolerance vs coded `0.001`; Day 10 `"runnable demos"` module docstring vs `__main__` that raises (`log_loss.py:1` vs `:26-27`). Re-read the docstring against the code before finishing.
- **PEP 8 whitespace slips — spaces before `:`/`,`, missing space after `,` (days 7, 9, 10, 11):** Day 7 inconsistent punctuation/spacing; Day 9 (`p*math.log(p , base)`, `open(path , ...)`, `try :`, `elif level==`); Day 10 (`len(p) == 0 :`, `if px > 0 :`, `zip(p,q)`, `math.log(qx ,base)`, `px/qx , base`, `for y , p`, trailing whitespace `log_loss.py:12,17`); Day 11 (`(x[i] , y[i])`, `(i , mi)`, `key= lambda pair : pair[1] ,`, `x ,y =`, double-space `p_y  =`, trailing blank lines at EOF).
- **UPPERCASE accumulator for a plain local (days 9, 10, 11):** Day 9 `H` (`entropy.py:41`); Day 10 `Output`/`KL_D`; Day 11 `MI` (`mutual_information.py:35,45,54`). Lowercase reads as a variable; uppercase reads as a class or constant — use `total`/`mi`.
- **Error-message wording — grammar, capitalization, accuracy, spelling (days 7, 9, 10, 11):** Day 7 message typos; Day 9 grammar slips and a message that also fires at 0; Day 10 inaccurate `"both p and q are empty"` (`cross_entropy.py:9`), agreement/grammar slips (`:12`, `:15`, `"an value"` at `:25,:37`), vague `"label should be 1"` (`log_loss.py:13`), and mixed capitalization (`:9` vs `:12`); Day 11 spelling typos (`"the fuction requre equal length"`, `"...the fuction doesn't accept"`, `"same lenth"`) plus messages that don't state the rule the docstring states. Absorbs the narrower spelling-typo count, which reached 3 days (7, 8, 11).
